/* #################  MAIN JS  ################# */



$(document).ready(function(){ // Ready Start

	$(".carousel").slick({
		dots: false,
		autoplay: true,
		autoplaySpeed: 5000,
	    infinite: true,
	    speed: 1500,
	    fade: true,
	    pauseOnHover: false,
	    cssEase: 'linear'
	});
	
}); // Ready End

$(window).load(function(){ // Load Start
	
	
}); // Load End

$(window).resize(function(){ // Resize Start
	
	
}); // Resize End